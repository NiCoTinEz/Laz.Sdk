<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Laz.Sdk</RootNamespace>
    <AssemblyName>Laz.Sdk</AssemblyName>
    <PackageId>Laz.Sdk</PackageId>
    <Version>0.1.0</Version>
    <Description>Unofficial .NET 10 client for the Lazada Open Platform REST API. Async-only, dependency-injection-first, multi-tenant.</Description>
    <IsPackable>true</IsPackable>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Http" Version="10.0.7" />
    <PackageReference Include="Microsoft.Extensions.Options.ConfigurationExtensions" Version="10.0.7" />
    <PackageReference Include="Microsoft.Extensions.Options.DataAnnotations" Version="10.0.7" />
    <!-- SourceLink only on CI (or when a remote is wired) to keep local pack quiet pre-publish. -->
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="8.0.0" PrivateAssets="all" Condition="'$(ContinuousIntegrationBuild)' == 'true'" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>
